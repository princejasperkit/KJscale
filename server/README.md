server: Add Express endpoint to accept applications, generate reference, and email admin + applicant

Files added:
- server/server.js
- package.json
- .env.example
- .gitignore

Notes:
- Configure SMTP settings in environment variables before running.
- Start server with: npm install && npm start
